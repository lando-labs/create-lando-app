# Sprint Manifest: Front-door handoff DevX

> Generated: 2026-07-28
> Started: 2026-08-11
> Status: **Active**
> Plan: `plans/2026-07-28-plan.md`
> Decision (2026-08-11): **#53 folded into this sprint** (not a standalone v0.1.1) —
> it reworks the exact slate-`:root` machinery #51 touches; one v0.2.0 release.

## Overview
- **Branch**: `sprint/v0.2.0-handoff-devx`
- **Target Version**: v0.1.0 → **v0.2.0** (MINOR)
- **Focus Area**: The AI handoff (Templates + AI-briefing-layer)
- **Estimated Scope**: Large but coherent (multi-session; one deliverable)

## The problem (validated in the wild)
The getting-started page hands users off to their AI, but the handoff is rough:
the page masks its saved theme (slate `:root` override) so the theme looks broken
until the page is deleted into a void; cold "build X" prompts skip orientation; and
the "protected layout" framing makes the AI route around `layout.tsx` by reflex.
`sst-salesiq` (a real app on the scaffold) rebuilt the entire preview as a
`/theme-preview` route to work around the masking — its own commit says so.

## Issues Addressed
- [ ] **#51** — Smoother AI handoff (expanded this sprint):
  - [ ] Orient-first prompt sequence (orient → theme+first-screen → more)
  - [ ] AI builds a *visible* first screen on handoff, replacing `app/page.tsx` +
        deleting `app/_starter/` (`:root` mask self-resolves — no mechanism change)
  - [ ] Keepable `/theme-preview` route (reuse the `_starter` gallery) instead of a
        from-scratch rebuild
  - [ ] Brief clarifies what's protected in `app/layout.tsx` (CSS import order +
        `themeScript()` only) and *recommends* the `app/(app)` route group on purpose
  - [ ] "Stays neutral on purpose" reassurance copy on the page
  - [ ] Clean stale `providers.tsx` comments referencing the deleted page
- [ ] **#53** (folded in) — Getting-started page poisons `localStorage` with the slate
      baseline, so after configure→reload the app loads the wrong colours (slate shadows
      `defaultProductTheme={brandTheme}` via the DS's `storedProductTheme || default`):
  - [ ] `ColorFoundation` persists **no throwaway theme** when Apply-to-page is OFF
        (don't call `setProductTheme(SLATE_BASELINE)` — leave the ThemeProvider default)
  - [ ] Belt-and-suspenders: clear any stale `PRODUCT_THEME_KEY` on page mount
  - [ ] Apply-to-page still persists the user's chosen theme (cleared on toggle-off/unmount)
  - [ ] The #51 rework (visible first screen replaces the page) dissolves the root cause
        too — these two share the slate-masking origin, which is why they ship together

## Acceptance Criteria
- [ ] Handoff section leads with an **orient** prompt, then a **theme + first-screen**
      prompt (save + wire providers + build first screen + delete `_starter` in one
      paste), then further starters — a sequence, not a flat list.
- [ ] `AGENTS.md` / `START_HERE.md` instruct the AI on first handoff: orient → wire the
      theme → build a **visible** first screen; and teach what's protected in
      `layout.tsx` + the `app/(app)` route-group pattern for app chrome.
- [ ] A keepable theme-QA surface exists (route or clear opt-in), not a forced rebuild.
- [ ] No stale references to the getting-started page in shipped files.
- [ ] **#53:** loading the getting-started page persists NO throwaway theme; after
      configure→reload the app loads the wired brand theme (not slate), no incognito.
- [ ] DS primitives only; Server Component preserved; `--no-mcp` path intact.
- [ ] Browser-verified (real handoff walkthrough); `npm test` green (#462 + AI wiring).

## Dependencies
- None external (DS 0.59 + MCP public). Supersedes the "talk-to-your-AI" half of #14.

## Parallel Work Considerations
### Sprint scope (work here)
- `templates/next-app-router/app/page.tsx` (handoff section + reassurance copy)
- `templates/next-app-router/{AGENTS.md, START_HERE.md}` (brief edits)
- `templates/next-app-router/app/providers.tsx` (comment cleanup)
- `templates/next-app-router/app/_starter/**` (if converting to a keepable route)
- `scripts/smoke.mjs` (assert the new handoff structure)
### Protected / avoid
- The #462 CSS import order in `app/layout.tsx` (do NOT touch — the whole point).
- `src/scaffold.ts` — that's Sprint 2 (#52), keep out to avoid conflicts.

## Git Workflow
- Branch from: `main`
- Branch: `sprint/v0.2.0-handoff-devx`
- Commit prefix: `[template]`
- Design first (`adaptive-ux-architect` drafts copy/brief) → implement
  (`nextjs-lando-ds`) → browser-verify → smoke.

## Rollback Plan
- Atomic commits per sub-item; the handoff copy, the brief edits, and the theme-QA
  route are independently revertible.
