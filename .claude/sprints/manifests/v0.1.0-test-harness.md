# Sprint Manifest: Test Harness + Drift Automation

> Generated: 2026-07-12
> Status: ~~Planning~~ **Active**
> Started: 2026-07-12 · Hydrated: 2026-07-12
> Plan: `plans/2026-07-12-plan.md`

## Overview
- **Branch**: `sprint/v0.1.0-test-harness`
- **Target Version**: 0.1.0 (unreleased — dev hardening, no publish this sprint)
- **Focus Area**: CI / smoke test / drift automation
- **Estimated Scope**: Medium (1–2 sessions)

## Issues Addressed
- [ ] #1: Build the consumer smoke test (`scripts/smoke.ts`)
- [ ] #2: Wire DS→CLI drift auto-PR (repository_dispatch receiver)

## Acceptance Criteria
- [ ] `scripts/smoke.ts` scaffolds the `next-app-router` template into a temp
      dir, reusing the CLI's copy + placeholder-substitution logic (no drift
      between CLI and smoke).
- [ ] Smoke test is **dormant-safe**: when the DS can't be resolved from npm it
      SKIPS with a clear message and exits 0 (so CI stays green pre-A1).
- [ ] `npm test` wired to run the smoke test; still green in current pre-A1 state.
- [ ] Drift receiver workflow (`.github/workflows/ds-drift.yml`) triggers on
      `repository_dispatch` (type `ds-published`), bumps the `DS_VERSION`
      constant in `src/index.ts`, and opens a PR.
- [ ] The drift PR body explains it must pass the smoke test before merge.
- [ ] Existing CI (`Typecheck / Build / Test`) stays green.

## Dependencies
- None. Both deliverables are built **dormant** and flip live in Sprint 2 (A1).
- Sibling half of drift (the DS-side dispatcher) is tracked in the DS repo — out
  of scope here.

## Parallel Work Considerations

### Sprint Scope (work happens here)
- `scripts/smoke.ts` (new)
- `package.json` (`test` script)
- `.github/workflows/ds-drift.yml` (new)
- Possibly a small refactor of `src/index.ts` to export the copy/substitute
  logic so the smoke test reuses it (avoid divergence).

### Protected Areas (do NOT touch this sprint)
- The #462 golden-path template CSS (`app/layout.tsx`, `app/globals.css`) —
  changing it defeats the point of the guard we're building.
- `publish.yml` — Sprint 2 territory.

## Git Workflow
- Branch from: `main`
- Branch name: `sprint/v0.1.0-test-harness`
- Commit prefix: `[ci]` (smoke/drift), `[cli]` (if refactoring exports)
- Manifest travels with the PR (committed to the sprint branch, not `main`).

## Rollback Plan
- Atomic commits per issue (#1 smoke, #2 drift).
- All new scaffolding is dormant; reverting either file is side-effect-free
  pre-A1.

## Notes
- **Local install caveat**: a scaffolded app can't `npm install` until the DS is
  on public npm. The smoke test must therefore support a DS **tarball path**
  input (env var / arg) so it can be exercised locally against a DS `npm pack`
  before A1, and against public npm after.

---

## Full Issue Details

> Hydrated on: 2026-07-12 · Source: GitHub Issues (lando-labs/create-lando-app)

### Issue #1: Build the consumer smoke test (`scripts/smoke.ts`)
**Labels**: `blocked:a1`, `ci-cd` · **State**: Open

The real guard for the #462 cascade-layer contract. A plain `DS_VERSION` bump
can't catch a layer-contract break — only a build-and-assert can.

**What it does**
1. Scaffold the `next-app-router` template into a temp dir (reuse the CLI's copy
   + placeholder-substitution path).
2. `npm install` against the target DS version (or a local DS tarball in CI).
3. `next build`.
4. Assert DS component spacing survives the `@layer app-reset` reset (the #462
   invariant) — e.g. computed padding/margin on a rendered DS component is
   non-zero.

**Wiring**: replace the `npm test` placeholder; CI runs it on PR/push; drafted
**dormant** now (skips cleanly when the DS can't be resolved), flipped live at A1.

### Issue #2: Wire DS→CLI drift auto-PR (repository_dispatch receiver)
**Labels**: `blocked:a1`, `ci-cd` · **State**: Open

Keep templates in sync with the DS layer contract via **CI push, not pull**.

**Receiver side (this repo)**: a workflow triggered by `repository_dispatch`
(type `ds-published`) that bumps `DS_VERSION` in `src/index.ts`, opens a PR, and
the PR runs the consumer smoke test (#1) as the real drift guard.

**Dispatcher side (DS repo — sibling half)**: the DS `publish.yml` adds a
`repository_dispatch` step targeting this repo on release. Out of scope here.

### Build order (dependency)
#2 depends on #1 (the drift PR's value is that it runs the smoke test). Build the
shared scaffold module → smoke test → drift workflow.
